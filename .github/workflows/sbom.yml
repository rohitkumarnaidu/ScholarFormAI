name: SBOM Generation

on:
  push:
    branches: [main]
    paths:
      - 'backend/requirements*.txt'
      - 'frontend/package*.json'
      - 'frontend/yarn.lock'
  schedule:
    - cron: '0 8 * * 1'  # Every Monday 08:00 UTC
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  generate-sbom:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405
        with:
          python-version: '3.12'

      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: '20'

      - name: Install Python deps for SBOM
        run: |
          pip install pip-licenses cyclonedx-bom
          pip install -r backend/requirements.txt

      - name: Install npm deps
        working-directory: ./frontend
        run: npm ci

      - name: Generate CycloneDX SBOM (Python)
        run: |
          pip freeze > /tmp/pip-freeze.txt
          cyclonedx-py requirements /tmp/pip-freeze.txt --output-format json -o sbom/backend-sbom.json

      - name: Generate CycloneDX SBOM (npm)
        working-directory: ./frontend
        run: |
          npx --yes @cyclonedx/cyclonedx-npm --output-format JSON --output-file ../sbom/frontend-sbom.json

      - name: Generate SPDX SBOM (combined)
        run: |
          pip install spdx-tools
          python -c "
          import json
          import glob
          # Merge CycloneDX files into a summary report
          backend = json.load(open('sbom/backend-sbom.json'))
          frontend = json.load(open('sbom/frontend-sbom.json'))
          summary = {
            'generated': '2026-06-14T00:00:00Z',
            'backend_packages': len(backend.get('components', [])),
            'frontend_packages': len(frontend.get('components', [])),
            'total_packages': len(backend.get('components', [])) + len(frontend.get('components', [])),
          }
          json.dump(summary, open('sbom/summary.json', 'w'), indent=2)
          "

      - name: Create PR for SBOM updates
        id: pr
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add sbom/
          if git diff --cached --quiet; then
            echo "No SBOM changes to commit."
            exit 0
          fi
          BRANCH="sbom-update-$(date +%s)"
          git checkout -b "$BRANCH"
          git commit -m "chore: update SBOM (auto)"
          git push origin "$BRANCH"
          gh pr create \
            --base main \
            --head "$BRANCH" \
            --title "chore: update SBOM (auto)" \
            --body "Automated SBOM regeneration triggered by dependency changes." \
            --label dependencies
